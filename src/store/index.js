import Vue from 'vue'
import Vuex from 'vuex'
import routerStorage from './base/routerStorage.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    routerStorage
  }
})
